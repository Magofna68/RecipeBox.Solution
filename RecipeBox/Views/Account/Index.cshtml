@{
  Layout = "_Layout";
}

@using System.Security.Claims

<h2>Account Authentication:</h2>
<h4>with Identity</h4>
<hr />
@if (User.Identity.IsAuthenticated)
{
  <h2>Hello @User.Identity.Name!</h2>
  @* @using (Html.BeginForm("LogOff", "Account"))
  {
  <input type="submit" class="btn btn-default" value="Log out" />
  } *@
  @* <p>@Html.ActionLink("Home", "Index", "Home")</p> *@
  <p>@Html.ActionLink("See all recipies", "Index", "Recipies")</p>
  <p>@Html.ActionLink("See all categories", "Index", "Categories")</p>
  <p>@Html.ActionLink("Add new category", "Create", "Categories")</p>
  <p>@Html.ActionLink("Add new recipies", "Create", "Recipies")</p>

}
else
{
  <p>@Html.ActionLink("Register", "Register")</p>
  <p>@Html.ActionLink("Log in", "Login")</p>
  @* <p>@Html.ActionLink("Home", "Index", "Home")</p> *@
}